# Debug Log - 2026-05-12

## Round A - Intentional Exception Audit Close

- Added `# intentional:` comments at four HANA UI boundary exception handlers:
  `ConnectionTab.test_hana`, `HanaBrowserTab.load_schemas`,
  `HanaBrowserTab.on_tree_click`, and `HanaBrowserTab.search_hana_tables`.
- Behavior unchanged. Acceptance criterion: `pytest tests/ -q` remains at 440 passed.

## Round B - HANA Reconnect Guard

- Added `HANAConnector._reconnect_if_stale()` to ping HANA with `SELECT 'OK' FROM DUMMY`
  before `fetch_table_chunked` starts work.
- Reconnects only when the ping reports `-10821` or `Session not connected`; other ping
  failures still propagate.
- Added four unit tests for no connection, healthy connection, stale `-10821` reconnect,
  and non-stale error propagation. Acceptance criterion: `pytest tests/ -q` reaches
  444 passed.
